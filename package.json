{
  "name": "content-backend",
  "version": "1.0.0",
  "description": "backend for page content manager",
  "main": "app.js",
  "scripts": {
    "docker": "docker-compose up -d",
    "dir": "(mkdir public && mkdir public/files && mkdir public/files/photos && mkdir public/files/temp)",
    "local": "(bash scripts/env.sh && nodemon src/server.js)"
  },
  "author": "adrian",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "joi": "^17.13.3",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
